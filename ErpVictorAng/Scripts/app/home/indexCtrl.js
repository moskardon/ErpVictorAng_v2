(function () {
    'use strict';
    var controllerId = 'indexCtrl';

    erpApp.controller(controllerId, ['$scope', 'ngDialog', 'modalWindowFactory', 'notificationService', indexCtrl]);

    function indexCtrl($scope, ngDialog, modalWindowFactory, notificationService) {
        
    }
})();